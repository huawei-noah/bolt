name: linux-x86-avx2

on: push

jobs:
  linux:
    name: linux-x86-avx2
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v2
      - name: linux x86 compilation
        run: |
          ./install.sh --target=linux-x86_64_avx2 -t 32
